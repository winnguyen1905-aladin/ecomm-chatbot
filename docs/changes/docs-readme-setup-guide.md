# docs: README setup guide

The README was a single line with no setup instructions.

## What changed
- Documented all required env vars (Telegram token, DB_*).
- Added a Docker Compose + Air quickstart.
- Added an `.env.example` template.

## Impact
New contributors can run the project without reading the source.
